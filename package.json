{
  "name": "pinbot-defense",
  "version": "1.0.0",
  "description": "PinBot Defense - Tower defense game with robots that shoot pins",
  "main": "index.js",
  "scripts": {
    "build": "cp pinbot-defense.html www/index.html",
    "sync": "npm run build && npx cap sync",
    "open:ios": "npm run sync && npx cap open ios",
    "open:android": "npm run sync && npx cap open android"
  },
  "license": "GPL-3.0",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.9.3"
  }
}
